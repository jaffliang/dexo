# Xcode
*.xcodeproj
*.xcworkspace
xcuserdata/

# Tuist
Derived/
Tuist/.build/
TODO.md
# Build
DerivedData/
build/
.build/

# Local config (contains DEVELOPMENT_TEAM etc.)
.mise.local.toml
server/push-relay/
*.p8

# XcodeBuildMCP local config (machine-specific simulator UUID)
.xcodebuildmcp/

# macOS
.DS_Store
*.swp
*~

# CocoaPods (if ever used)
Pods/

# Local scratch / reference docs
doc/

# DoH gateway build products (rebuilt by scripts/build-doh-gateway.sh)
Native/DoHGateway/target/
Native/DoHGateway/dist/
Packages/DoHGatewayPolicy/.build/

